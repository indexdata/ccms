drop project

Remove a project

drop project project_name

Description

The drop project command removes a project from the system. This automatically
drops the object table and deletes all of its data. All associated project
properties are also removed.

Warning drop project removes a project permanently. The operation cannot be
        undone.

Parameters

project_name The name of the project.

Examples

Remove a project myproj:

drop project myproj;

