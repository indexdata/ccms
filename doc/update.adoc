==== update

Update object attributes

[source,subs="verbatim,quotes"]
----
update `**_set_name_**`
    set `**_attribute_**` = { `*_value_*` | null } [, ...]
    where id = `*_object_id_*`
----

[discrete]
===== Description

The `update` command changes the value of specified attributes for an
object having a specified ID.

Note that the values of updated attributes are shared by all sets
within a project.

[discrete]
===== Parameters

The first argument specifies a set to be updated.

[discrete]
====== set

The `set` clause specifies the attributes to set and their new values.

[discrete]
====== where

The `where` clause specifies the object ID.

[discrete]
===== Examples

To set the decision and fund in an object having ID = 12:

----
update myproj.myset set decision = true, fund = cultural where id = 12;
----
